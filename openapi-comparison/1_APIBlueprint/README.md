### 📂 `1_APIBlueprint/README.md`

````markdown
# API Blueprint Demo

Định dạng tài liệu dựa trên Markdown, ưu tiên khả năng đọc hiểu của con người.

### 🛠 Công cụ hỗ trợ

1. **Aglio**: Trình render tài liệu HTML đẹp mắt.
2. **Dredd**: Công cụ kiểm thử (Contract Testing).

### 🚀 Hướng dẫn chạy Demo

**1. Xem tài liệu (HTML):**

```bash
Công cụ giúp biến file mã nguồn "khô khan" .apib (viết bằng Markdown) thành một giao diện web (HTML) chuyên nghiệp, dễ đọc cho người dùng hoặc đối tác.
npm install -g aglio
aglio -i library.apib -s

Phần Kiểm thử - Contract Testing
### 🚀 Cách chạy
2. **Contract Testing:** Dùng `dredd` để so khớp file này với Flask server.
```
````
